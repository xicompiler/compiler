(library
 (name frontend)
 (libraries ast unicode position core sexpPrinter xiFile)
 (private_modules parser lexer)
 (instrumentation
  (backend bisect_ppx)))

(ocamllex lexer)

(menhir
 (modules parser))
